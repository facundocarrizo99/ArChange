[tool.poetry]
name = "archange"
version = "1.0.0"
description = "Argentine Exchange Rates API – fetch, store and serve ARS exchange rates"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
